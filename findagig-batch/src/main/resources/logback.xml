<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false">

    <!-- file generated by git-commit-id-plugin git-commit-id-plugin. check pom.xml -->
    <property resource="git.properties"/>

    <!-- spring-boot properties now can be accessed here -->
    <property resource="application.properties"/>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="JSON_FILE" class="net.logstash.logback.appender.LoggingEventAsyncDisruptorAppender">
        <!-- ConsoleAppender is good for now, but must be replaced by "ch.qos.logback.core.rolling.RollingFileAppender" -->
        <appender class="ch.qos.logback.core.rolling.RollingFileAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <customFields>{"application":"findagig-batch","gitCommit":"${git.commit.id}" }</customFields>
            </encoder>

            <file>${logging.file.logback}</file>

            <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
                <fileNamePattern>${logging.file.logback}.%i.gz</fileNamePattern>
                <minIndex>1</minIndex>
                <maxIndex>5</maxIndex>
            </rollingPolicy>

            <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
                <maxFileSize>5MB</maxFileSize>
            </triggeringPolicy>
        </appender>
    </appender>

    <root level="info">
        <appender-ref ref="JSON_FILE"/>
        <appender-ref ref="STDOUT"/>
    </root>

</configuration>